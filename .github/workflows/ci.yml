name: CI

on:
  push:
    branches: [main]
  pull_request:
  workflow_dispatch:

jobs:
  emacs-tests:
    name: ERT (Emacs ${{ matrix.emacs_version }})
    runs-on: ubuntu-latest
    strategy:
      fail-fast: false
      matrix:
        # cerebro.el targets Emacs 28+; test the floor and a current release.
        emacs_version: ['28.2', '30.1']
    steps:
      - uses: actions/checkout@v4

      - uses: purcell/setup-emacs@master
        with:
          version: ${{ matrix.emacs_version }}

      - name: Byte-compile
        run: emacs --batch -L emacs -f batch-byte-compile emacs/cerebro.el

      - name: Run tests
        run: emacs --batch -L emacs -l cerebro-test -f ert-run-tests-batch-and-exit

  bash-tests:
    name: Bash suites (ubuntu-latest)
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - name: Run every tests/*.sh
        shell: bash
        run: |
          set -uo pipefail
          failed=()
          for t in tests/*.sh; do
            echo "::group::$t"
            if bash "$t"; then
              echo "::endgroup::"
            else
              echo "::endgroup::"
              echo "::error file=$t::$t failed"
              failed+=("$t")
            fi
          done
          if [ "${#failed[@]}" -gt 0 ]; then
            printf 'FAILED: %s\n' "${failed[@]}" >&2
            exit 1
          fi
          echo "all bash suites passed"

      - name: Lint (advisory, never blocks)
        continue-on-error: true
        run: bash scripts/lint
