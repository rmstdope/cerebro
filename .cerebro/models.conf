# Which model each agent runs on, overriding what its `agents/<role>.md` declares.
#
# <agent-name | role | default>   <model | ->   [effort]
default     opus    medium
implementer opus    low
planner     opus    medium
verifier    opus    medium
architect   fable   medium
feedback    opus    low
