#!/usr/bin/env bash
# Starts Psylocke, the verifier: a name for `scripts/launch Psylocke`, kept because the docs and
# the navigator's fingers know it. Everything it does lives in `launch`; nothing here may grow.
exec "$(cd "$(dirname "${BASH_SOURCE[0]}")" && pwd)/launch" Psylocke "$@"
